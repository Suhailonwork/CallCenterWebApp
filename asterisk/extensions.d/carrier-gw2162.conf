[from-internal]
exten => _13[6-9]XXXXXXXXX,1,NoOp(Outbound mobile via gw2162)
 same  => n,Set(DNUM=${EXTEN:2})
same => n,Progress()
same => n,Ringing()
 same  => n,Dial(PJSIP/${DNUM}@gw2162,60,,Rr)
 same  => n,Hangup()
